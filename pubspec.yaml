name: fake_store_api_package
description: "With project you can create a fake api server with a few lines of code"
version: 0.0.1
homepage: null

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  dartz: ^0.10.1
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  http: ^1.4.0

dev_dependencies:
  build_runner: ^2.4.15
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  mockito: ^5.4.6

flutter: null
